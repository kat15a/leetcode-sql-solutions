-- LeetCode 1070: Product Sales Analysis III
-- Difficulty: Easy
-- Topic: Subquery, GROUP BY, Tuple Comparison

SELECT product_id,
       year AS first_year,
       quantity,
       price
FROM Sales
WHERE (product_id, year) IN (
    SELECT product_id,
           MIN(year)
    FROM Sales
    GROUP BY product_id
);
