-- LeetCode 1729: Find Followers Count
-- Difficulty: Easy
-- Topic: GROUP BY, COUNT, ORDER BY

SELECT user_id,
       COUNT(follower_id) AS followers_count
FROM Followers
GROUP BY user_id
ORDER BY user_id;
